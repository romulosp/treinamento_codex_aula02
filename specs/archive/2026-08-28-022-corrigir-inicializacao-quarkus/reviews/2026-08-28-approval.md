# Aprovação: 022-corrigir-inicializacao-quarkus

## Pré-condições

- SPEC: `SPEC_APROVADA`.
- Implementação: `IMPLEMENTADA`.
- Revisão da implementação: `IMPLEMENTACAO_APROVADA`.
- Validação: `VALIDADA`.

## Decisão

`APR-001` — aprovar a configuração do goal `build` do plugin Quarkus, pois `mvn test` passou e `mvn quarkus:dev` iniciou o servidor na porta 8080.

## Veredito

`APROVADA`

Commit inicial: `98ece3b`.
