# Design

Corrigir dependências exclusivamente de teste no Maven. Adicionar integração HTTP em H2 isolado no perfil test. Verificar CRUD PostgreSQL usando registro exclusivo e limpeza pelo ID criado. Corrigir eventual conflito entre rotas React e proxy por negociação do cabeçalho Accept. Usar instalações Java/Maven existentes, sem configuração global.
