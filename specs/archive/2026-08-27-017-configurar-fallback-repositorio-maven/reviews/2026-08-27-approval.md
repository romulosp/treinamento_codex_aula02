# Aprovação da mudança: 017-configurar-fallback-repositorio-maven

## Pré-condições verificadas

- SPEC aprovada em `2026-08-27-spec-review.md`.
- Implementação aprovada em `2026-08-27-implementation-review-resubmissao.md`.
- Validação aprovada em `validation.md`.
- A verificação real do Nexus depende de VPN corporativa, mas não há pendência bloqueante no fallback público validado.

## Decisão

`APR-001` — Aprovar a configuração de fallback Maven, pois o caminho público resolve dependências e executa a suíte com repositório temporário, sem modificar configurações globais ou expor segredos.

## Veredito

`APROVADA`
