# Aprovação: 021-definir-diretorio-projeto-gerado

## Pré-condições

- SPEC: `SPEC_APROVADA`.
- Implementação documental: `IMPLEMENTADA`.
- Revisão da implementação: `IMPLEMENTACAO_APROVADA`.
- Validação: `VALIDADA`.

## Evidências

- `REV-001` a `REV-003`: SPEC clara, completa e testável.
- `IMP-REV-001` a `IMP-REV-004`: documentação vigente aderente à regra de diretório por projeto.
- `VAL-001` a `VAL-003`: busca textual, derivação dos dois exemplos e verificação de formatação aprovadas.
- A alteração preexistente em `specs/archive/2026-08-28-019-gerenciar-tarefas/reviews/2026-08-28-approval.md` foi preservada e excluída do escopo.

## Decisão

`APR-001` — aprovar a definição de `apps/backend/<artifactId-sem-hifens>/` como diretório de cada projeto Java independente, mantendo `apps/backend/` apenas como contêiner local.

## Veredito

`APROVADA`

Commit inicial: `9fa8c3b`.
